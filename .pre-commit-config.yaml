repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version.
    rev: v0.9.1  # Use the latest version
    hooks:
      # Linter hook
      - id: ruff
        args: [ --fix ]
      # Formatter hook
      - id: ruff-format
